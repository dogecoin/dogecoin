#!/bin/bash 
POST_STRING=$(cat)
echo 'Content-type: text/html'
echo


this.is.fun() {

: this is excuted via a the bash shell
: it will verify and only run if it-s
: hash in on the allowed list. 
date
}

pretty.print() {

echo '<hr><pre>'
eval $1
echo '</pre><hr>'
}

echo $POST_STRING > outfile.txt

declare -f this.is.fun | sum

env | egrep 'ADDR|COOKIE|QUERY|REQUEST'

echo '</pre>'
# $repo_dir = '/home/dime/dimecash';
# $unspendable = '/home/dime/unspendable/unspendable.py';

echo "
<form action='decode.cgi' method='post'>
   <p>Name:  <input type='text' name='name' size=60  value='xxx'   /> 
   <p>Email: <input type='text' name='email' size=60 value='yyy'  /> </p>
   <p><textarea rows='20' cols='60' 
   name='function'>this.is.fun () 
{  
    : this is excuted via a the bash shell;
    : it will verify and only run if it-s;
    : hash in on the allowed list.;
    date
}
  </textarea> 
   <input type='submit' name='submit' value='Submit' />
</form>
</div><hr>";

while read LINE
   do
   echo $LINE
   done
