aws.ansible.hosts () 
{ 
    echo "[$1]" > hosts;
    . aws.public-ips;
    aws.public-ips | while read HOST; do
        ssh -i ./id_rsa -n -oStrictHostKeyChecking=no ubuntu@$HOST exit;
        echo ubuntu@$HOST ansible_ssh_private_key_file=./id_rsa >> hosts;
    done
}
