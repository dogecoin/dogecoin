aws.public-ips () 
{ 
    cat regions.txt | while read REGION; do
        cd $REGION;
        terraform show | grep public_ip | grep -v associate | while read A B C; do
            echo $C;
        done | xargs -n1;
        cd ..;
    done
}
